functions.php của unikey:
    $requires = [
        '/base/tailwindcss/set-up.php',
        '/base/ux-builder/set-up.php', // nếu dùng ux builder shortcode
    ];

    foreach ($requires as $file) {
        require_once get_stylesheet_directory() . $file;
    }
copy thư mục base cho child theme
copy các file cho child theme:
    package.json
    postcss.config.mjs
    tailwind.config.js
    vite.config.js
npm install
truy cập thư mục child theme:
    cấu hình wp-config.php > WP_DEBUG > true nếu đang dev ở local / false nếu cần build cho prod
    active child theme cần dev
    npm run dev/npm run build
    